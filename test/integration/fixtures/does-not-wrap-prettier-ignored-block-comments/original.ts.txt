if (ready) {
  // prettier-ignore
  /* This block comment should stay on one long line because the ignore marker applies to the following comment block. */
  run(    );
  // The line above should be formatted
}

if (documented) {
  // prettier-ignore
  /** This JSDoc comment is already ignored by the plugin, so the ignore marker should keep applying to the following code. */
  run(    );
  // The line above should stay ignored
}
