const view = (
  <section>
    <span>
      {
        // This trailing comment must stay
        // inside its JSX expression even though
        // it exceeds the print width.
        value
      }
    </span>
  </section>
);

const message = `prefix
${
  // This trailing comment must stay inside its
  // template interpolation even though it
  // exceeds the print width.
  value
}`;

const parenthesizedView = (
  <span>
    {
      // This parenthesized trailing JSX comment
      // must move before the root and wrap in
      // one formatting pass.
      value
    }
  </span>
);

const parenthesizedMessage = `prefix
${
  // This parenthesized trailing template
  // comment must move before the root and wrap
  // in one formatting pass.
  value
}`;
