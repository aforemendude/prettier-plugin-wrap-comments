// eslint-disable-next-line @typescript-eslint/no-explicit-any
// This trailing comment should still move above the
// statement and wrap even when there is a directive
// directly above.
const value: any = 1;
