function demo() {
  // This trailing comment should move above the
  // statement and wrap using the statement indentation
  // when the original line exceeds the configured print
  // width.
  const value = 1;
}
