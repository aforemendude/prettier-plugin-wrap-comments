function demo(enabled: boolean) {
  const config = {
    enabled,
    // This comment describes the completed config object
    // and intentionally moves before the closing
    // declaration delimiter.
  };
  if (enabled) {
    runTask();
    // This comment describes the completed conditional
    // block and intentionally moves before the closing
    // block delimiter.
  }
  runTask(
    config,
    // This comment describes the completed function call
    // and intentionally moves before the closing call
    // delimiter.
  );
}
